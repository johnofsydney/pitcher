<%= form_with(model: webhook) do |form| %>
  <% if webhook.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(webhook.errors.count, "error") %> prohibited this webhook from being saved:</h2>

      <ul>
        <% webhook.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :customer_id, style: "display: block" %>
    <%= form.text_field :customer_id %>
  </div>

  <div>
    <%= form.label :document_id, style: "display: block" %>
    <%= form.text_field :document_id %>
  </div>

  <div>
    <%= form.label :customer_url, style: "display: block" %>
    <%= form.text_area :customer_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
